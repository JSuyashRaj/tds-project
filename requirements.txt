# requirements.txt - Fixed for Render deployment
setuptools>=70.0.0
pip>=24.0
wheel>=0.42.0

# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-dotenv==1.0.0
pydantic==2.6.0

# Core ML/AI packages - specific versions with wheels
numpy==1.24.4
scipy==1.11.4
scikit-learn==1.3.2

# LangChain packages - specific working versions
langchain==0.1.20
langchain-community==0.0.38
langchain-core==0.1.52

# Google Gemini support
langchain-google-genai==1.0.1
google-generativeai==0.5.0

# Vector DB
faiss-cpu==1.8.0

# HuggingFace - avoid packages that need Rust compilation
# Use these specific versions that have pre-built wheels
transformers==4.36.2
tokenizers==0.15.0
huggingface-hub==0.20.0

# Sentence transformers - use older stable version
sentence-transformers==2.2.2

# PyTorch CPU-only version (smaller, pre-compiled)
torch==2.1.2+cpu --index-url https://download.pytorch.org/whl/cpu
torchvision==0.16.2+cpu --index-url https://download.pytorch.org/whl/cpu

# HTTP clients
httpx==0.25.2
requests==2.31.0